namespace {{this.Namespace}};

<!--{{[String.Append] "Left side -" "- Right side"}}-->

public class {{this.FileName}}_Should
{
{{#each this.PropertyInfos}}
    public void DoNotChange{{PropertyTypeName}}_Value() {
        
        // Arrange
        {{PropertyTypeName}} 
        
    }
{{/each}}
}