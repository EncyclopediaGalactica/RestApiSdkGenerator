namespace {{this.Namespace}};

/// <summary>
///     The {{this.TypeName}} object.
/// </summary>
public class {{this.TypeName}}
{
{{#each this.PropertyInfos}}
    /// <summary>
    ///     Gets or set the {{PropertyName}} property's value
    /// </summary>
    public {{PropertyTypeName}}{{#IsNullable}}?{{/IsNullable}} {{PropertyName}} { get; set; }
    {{#unless @last}}

    {{/unless}}
{{/each}}
}